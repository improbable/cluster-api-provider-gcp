
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.9
  creationTimestamp: null
  name: gcpmanagedmachinepools.exp.infrastructure.cluster.x-k8s.io
spec:
  group: exp.infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: GCPManagedMachinePool
    listKind: GCPManagedMachinePoolList
    plural: gcpmanagedmachinepools
    shortNames:
    - gmmp
    singular: gcpmanagedmachinepool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Cluster to which this GCPManagedMachinePool belongs
      jsonPath: .metadata.labels.cluster\.x-k8s\.io/cluster-name
      name: Cluster
      type: string
    - description: Cluster infrastructure is ready
      jsonPath: .status.ready
      name: Ready
      type: string
    - description: Provider status
      jsonPath: .status.providerStatus
      name: ProviderStatus
      type: string
    name: v1alpha3
    schema:
      openAPIV3Schema:
        description: GCPManagedMachinePool is the Schema for the gcpmanagedmachinepools
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: GCPManagedMachinePoolSpec defines the desired state of GCPManagedMachinePool
            properties:
              autoscaling:
                description: Autoscaling contains the autoscaling config for the pool.
                  If specified, the MachinePool replicas is ignored. This does not
                  match the cluster-api contract for MachinePool that expects a fixed
                  size.
                properties:
                  maxNodeCount:
                    description: 'MaxNodeCount: Maximum number of nodes in the NodePool.
                      Must be >= minNodeCount.'
                    format: int64
                    minimum: 1
                    type: integer
                  minNodeCount:
                    description: 'MinNodeCount: Minimum number of nodes in the NodePool.
                      Must be >= 1 and <= maxNodeCount.'
                    format: int64
                    minimum: 1
                    type: integer
                required:
                - maxNodeCount
                - minNodeCount
                type: object
              bootDiskSizeGB:
                description: BootDiskSizeGB is the disk size for every machine in
                  this pool. If you specify 0, it will apply the default size.
                format: int64
                type: integer
              diskType:
                description: 'DiskType is the type of boot disk for machines in this
                  pool. Possible values include: ''pd-standard'', ''pd-ssd''. Defaults
                  to ''pd-standard''.'
                enum:
                - pd-standard
                - pd-ssd
                type: string
              instanceType:
                description: InstanceType is the type of the VMs in the node pool.
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels is an optional set of Kubernetes labels to add
                  to the node pool.
                type: object
              preemptible:
                description: Preemptible determines if the nodes are preemptible.
                  Defaults to false.
                type: boolean
              taints:
                description: Taints specifies the taints the node pool should have.
                items:
                  description: The node this Taint is attached to has the "effect"
                    on any pod that does not tolerate the Taint.
                  properties:
                    effect:
                      description: Required. The effect of the taint on pods that
                        do not tolerate the taint. Valid effects are NoSchedule, PreferNoSchedule
                        and NoExecute.
                      type: string
                    key:
                      description: Required. The taint key to be applied to a node.
                      type: string
                    timeAdded:
                      description: TimeAdded represents the time at which the taint
                        was added. It is only written for NoExecute taints.
                      format: date-time
                      type: string
                    value:
                      description: Required. The taint value corresponding to the
                        taint key.
                      type: string
                  required:
                  - effect
                  - key
                  type: object
                type: array
            required:
            - instanceType
            type: object
          status:
            description: GCPManagedMachinePoolStatus defines the observed state of
              GCPManagedMachinePool
            properties:
              errorMessage:
                description: Any transient errors that occur during the reconciliation
                  of Machines can be added as events to the Machine object and/or
                  logged in the controller's output.
                type: string
              errorReason:
                description: Any transient errors that occur during the reconciliation
                  of Machines can be added as events to the Machine object and/or
                  logged in the controller's output.
                type: string
              providerStatus:
                description: ProviderStatus is the status of the node pool as reported
                  by GKE
                type: string
              ready:
                description: Ready is true when the provider resource is ready.
                type: boolean
              replicas:
                description: Replicas is the most recently observed number of replicas.
                  This is currently ignored as the GKE API does not expose this without
                  looking at the underlying VMs.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
